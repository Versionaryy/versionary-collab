

# Set the correct application name for the Eureka server
spring.application.name=eureka-service

# The Eureka server must run on a fixed, well-known port
server.port=8761

# A standalone Eureka server should not try to register with itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false